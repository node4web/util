import { test, assert } from "vitest";
import * as index from "../src/index-default.js";

test("MIMEParams", () => {
  assert("MIMEParams" in index);
});
test("MIMEType", () => {
  assert("MIMEType" in index);
});
test("TextDecoder", () => {
  assert("TextDecoder" in index);
});
test("TextEncoder", () => {
  assert("TextEncoder" in index);
});
test("aborted", () => {
  assert("aborted" in index);
});
test("callbackify", () => {
  assert("callbackify" in index);
});
test("debug", () => {
  assert("debug" in index);
});
test("debuglog", () => {
  assert("debuglog" in index);
});
test("deprecate", () => {
  assert("deprecate" in index);
});
test("format", () => {
  assert("format" in index);
});
test("formatWithOptions", () => {
  assert("formatWithOptions" in index);
});
test("getSystemErrorMap", () => {
  assert("getSystemErrorMap" in index);
});
test("getSystemErrorName", () => {
  assert("getSystemErrorName" in index);
});
test("inherits", () => {
  assert("inherits" in index);
});
test("inspect", () => {
  assert("inspect" in index);
});
test("isDeepStrictEqual", () => {
  assert("isDeepStrictEqual" in index);
});
test("parseArgs", () => {
  assert("parseArgs" in index);
});
test("promisify", () => {
  assert("promisify" in index);
});
test("stripVTControlCharacters", () => {
  assert("stripVTControlCharacters" in index);
});
test("toUSVString", () => {
  assert("toUSVString" in index);
});
test("transferableAbortController", () => {
  assert("transferableAbortController" in index);
});
test("transferableAbortSignal", () => {
  assert("transferableAbortSignal" in index);
});
test("types", () => {
  assert("types" in index);
});
test("log", () => {
  assert("log" in index);
});
test("isUndefined", () => {
  assert("isUndefined" in index);
});
test("isSymbol", () => {
  assert("isSymbol" in index);
});
test("isString", () => {
  assert("isString" in index);
});
test("isRegExp", () => {
  assert("isRegExp" in index);
});
test("isPrimitive", () => {
  assert("isPrimitive" in index);
});
test("isObject", () => {
  assert("isObject" in index);
});
test("isNumber", () => {
  assert("isNumber" in index);
});
test("isNullOrUndefined", () => {
  assert("isNullOrUndefined" in index);
});
test("isNull", () => {
  assert("isNull" in index);
});
test("isFunction", () => {
  assert("isFunction" in index);
});
test("isError", () => {
  assert("isError" in index);
});
test("isDate", () => {
  assert("isDate" in index);
});
test("isBuffer", () => {
  assert("isBuffer" in index);
});
test("isBoolean", () => {
  assert("isBoolean" in index);
});
test("isArray", () => {
  assert("isArray" in index);
});
