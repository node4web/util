import { test, assert } from "vitest";
import * as index from "../../src/types/index-default.js";

test("isAnyArrayBuffer", () => {
  assert("isAnyArrayBuffer" in index)
});
test("isArrayBufferView", () => {
  assert("isArrayBufferView" in index)
});
test("isArgumentsObject", () => {
  assert("isArgumentsObject" in index)
});
test("isArrayBuffer", () => {
  assert("isArrayBuffer" in index)
});
test("isAsyncFunction", () => {
  assert("isAsyncFunction" in index)
});
test("isBigInt64Array", () => {
  assert("isBigInt64Array" in index)
});
test("isBigIntObject", () => {
  assert("isBigIntObject" in index)
});
test("isBigUint64Array", () => {
  assert("isBigUint64Array" in index)
});
test("isBooleanObject", () => {
  assert("isBooleanObject" in index)
});
test("isBoxedPrimitive", () => {
  assert("isBoxedPrimitive" in index)
});
test("isCryptoKey", () => {
  assert("isCryptoKey" in index)
});
test("isDataView", () => {
  assert("isDataView" in index)
});
test("isDate", () => {
  assert("isDate" in index)
});
test("isExternal", () => {
  assert("isExternal" in index)
});
test("isFloat32Array", () => {
  assert("isFloat32Array" in index)
});
test("isFloat64Array", () => {
  assert("isFloat64Array" in index)
});
test("isGeneratorFunction", () => {
  assert("isGeneratorFunction" in index)
});
test("isGeneratorObject", () => {
  assert("isGeneratorObject" in index)
});
test("isInt8Array", () => {
  assert("isInt8Array" in index)
});
test("isInt16Array", () => {
  assert("isInt16Array" in index)
});
test("isInt32Array", () => {
  assert("isInt32Array" in index)
});
test("isKeyObject", () => {
  assert("isKeyObject" in index)
});
test("isMap", () => {
  assert("isMap" in index)
});
test("isMapIterator", () => {
  assert("isMapIterator" in index)
});
test("isModuleNamespaceObject", () => {
  assert("isModuleNamespaceObject" in index)
});
test("isNativeError", () => {
  assert("isNativeError" in index)
});
test("isNumberObject", () => {
  assert("isNumberObject" in index)
});
test("isPromise", () => {
  assert("isPromise" in index)
});
test("isProxy", () => {
  assert("isProxy" in index)
});
test("isRegExp", () => {
  assert("isRegExp" in index)
});
test("isSet", () => {
  assert("isSet" in index)
});
test("isSetIterator", () => {
  assert("isSetIterator" in index)
});
test("isSharedArrayBuffer", () => {
  assert("isSharedArrayBuffer" in index)
});
test("isStringObject", () => {
  assert("isStringObject" in index)
});
test("isSymbolObject", () => {
  assert("isSymbolObject" in index)
});
test("isTypedArray", () => {
  assert("isTypedArray" in index)
});
test("isUint8Array", () => {
  assert("isUint8Array" in index)
});
test("isUint8ClampedArray", () => {
  assert("isUint8ClampedArray" in index)
});
test("isUint16Array", () => {
  assert("isUint16Array" in index)
});
test("isUint32Array", () => {
  assert("isUint32Array" in index)
});
test("isWeakMap", () => {
  assert("isWeakMap" in index)
});
test("isWeakSet", () => {
  assert("isWeakSet" in index)
});
